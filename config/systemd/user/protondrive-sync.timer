[Unit]
Description=Hourly safety-net Proton Drive sync
# Catches remote-side changes (web UI, phone) that inotify cannot see.

[Timer]
OnBootSec=5min
OnUnitActiveSec=1h
Persistent=true
RandomizedDelaySec=5min

[Install]
WantedBy=timers.target
